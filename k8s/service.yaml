apiVersion: v1
kind: Service
metadata:
  name: capstone-service
spec:
  selector:
    app: capstone-app
  ports:
    - protocol: TCP
      port: 80        
      targetPort: 5670 
  type: LoadBalancer  